/* #include<stdio.h>

int main() {
    unsigned int age;
    printf("Enter your age: "); // Prompting the user before input
    scanf("%u", &age);
    printf("Your age is: %u\n", age); // Corrected printf format
    return 0;
} */

#include <stdio.h>

int main(){
    char ch = "A";
    printf("%c",ch+1);
    return 0;
}